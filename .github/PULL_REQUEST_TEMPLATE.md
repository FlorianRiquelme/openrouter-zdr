## What

<!-- One or two sentences. Link the issue if there is one. -->

## Why

<!-- The problem this solves, from the user's point of view. -->

## Checklist

- [ ] Tests added or updated in `src/*.test.ts` (injected `fetch`, no network)
- [ ] `pnpm check` passes locally (lint, typecheck, test, build, `dist/` in sync)
- [ ] CHANGELOG.md updated under **Unreleased**
- [ ] No new runtime dependency; the ZDR pin is untouched
